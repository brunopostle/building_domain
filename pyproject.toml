[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "bsos"
version = "0.1.0"
requires-python = ">=3.12"
license = "GPL-3.0-or-later"
license-files = ["LICENSE", "DATABASE_LICENSE", "NOTICE"]
dependencies = [
    "pydantic>=2.0",
    "sqlmodel",
    "sqlalchemy",
    "alembic",
    "networkx",
    "typer",
    "instructor",
    "anthropic",
    "openai",
    "ifcopenshell",
    "sentence-transformers",
    "numpy",
    "scipy",
    "scikit-learn",
    "matplotlib",
    "structlog",
    "joblib",
    "mcp",
    "pypdf",
    "platformdirs",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
bsos = "bsos.cli.main:app"

[tool.hatch.build.targets.wheel]
packages = ["src/bsos"]

[tool.hatch.build.targets.wheel.shared-data]
"NOTICE" = "share/bsos/NOTICE"
"DATABASE_LICENSE" = "share/bsos/DATABASE_LICENSE"
